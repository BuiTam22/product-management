mixin search(keyword)
  form(id="form-search")
    .input-group
      input(
        id="input"
        type="text"
        placeholder="Nhập từ khóa"
        name="keyword"
        class="form-control"
        value=keyword
      )
      .input-group-append
        button(
          class="btn btn-success"
          type="submit"
        ) Tìm